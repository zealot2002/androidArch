package com.joy.common.service

/**
 * IProvider 服务 path。跨模块服务跳转请使用 [AppModuleService] 按接口类型获取，勿直接 build(path)。
 */
object ServiceConstants {
    const val SERVICE_SOCIAL_HOME_FRAGMENT = "/social/home_fragment"
}
